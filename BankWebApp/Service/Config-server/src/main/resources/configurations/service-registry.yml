eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: http://localhost:8761/eureka/

management:        # Enables circuit breaker health indicators
  endpoints:
    web:
      exposure:
        include: health
server:
  port: 8761